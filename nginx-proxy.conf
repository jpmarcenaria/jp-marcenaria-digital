# Nginx Reverse Proxy para JP Marcenaria Digital
# Este proxy encaminha requisições para o serviço "app" (porta 80) definido no docker-compose.
# Ajuste os domínios em "server_name" conforme sua configuração de DNS.

worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';
    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    client_max_body_size 16M;

    # Segurança básica
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Proxy para aplicação (container "app")
    upstream jp_marcenaria_app {
        server app:80;
        keepalive 32;
    }

    # HTTP
    server {
        listen 80;
        server_name jpmarcenaria.com www.jpmarcenaria.com jpmarcenaria.com.br www.jpmarcenaria.com.br;

        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }

        location / {
            proxy_pass http://jp_marcenaria_app;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Connection "";
        }
    }

    # HTTPS (opcional) — certs devem ser montados em /etc/nginx/ssl
    # Descomente este bloco quando possuir certificados válidos
    # server {
    #     listen 443 ssl http2;
    #     server_name jpmarcenaria.com www.jpmarcenaria.com jpmarcenaria.com.br www.jpmarcenaria.com.br;
    #     ssl_certificate /etc/nginx/ssl/fullchain.pem;
    #     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers HIGH:!aNULL:!MD5;
    #
    #     location /health {
    #         access_log off;
    #         return 200 "healthy\n";
    #         add_header Content-Type text/plain;
    #     }
    #
    #     location / {
    #         proxy_pass http://jp_marcenaria_app;
    #         proxy_http_version 1.1;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #         proxy_set_header Connection "";
    #     }
    # }
}

